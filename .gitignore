# Dataset — too large for GitHub (84MB), share separately via Google Drive
data/

# Intermediate pipeline outputs — regenerated by train.py
output/clean_data.parquet
output/processed_data.parquet

# NOTE: the following ARE tracked (needed to run the dashboard without retraining):
#   output/articles_with_topics.parquet  (28MB)
#   models/lda_model.joblib              (632KB)
#   models/count_vectorizer.joblib       (140KB)
#   models/topic_names.json              (1KB)

# Python environment
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# IDE
.vscode/
.idea/

# Test cache
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# Session & planning files (personal)
.claude/
